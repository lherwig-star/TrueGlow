// Die erste Zeile schreibt das Flutter-Werkzeug bei jedem Bau ohnehin selbst
// an den Anfang dieser Datei, sobald ein Podfile existiert - nachzulesen in
// packages/flutter_tools/lib/src/macos/cocoapods.dart,
// _addPodsDependencyToFlutterXcconfig. Sie steht hier nur deshalb im Repo,
// damit sichtbar ist, was sonst erst zur Laufzeit entsteht.
//
// Sie war NICHT die Ursache des Simulator-Fehlers vom 03.09.2026 - dieser
// Irrtum ist in DECISIONS 103 aufgeschrieben und dort auch richtiggestellt.
//
// `#include?` mit Fragezeichen: Die Datei entsteht erst bei `pod install`.
// Vor dem ersten Lauf gibt es sie nicht, und dann soll das kein Fehler sein.
#include? "Pods/Target Support Files/Pods-Runner/Pods-Runner.debug.xcconfig"
#include "Generated.xcconfig"
